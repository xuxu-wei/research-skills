interface:
  display_name: "Proposal Package Assembler"
  short_description: "Assemble the final proposal package from existing proposal, workflow state, context brief..."
  default_prompt: "Use $proposal-package-assembler for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
