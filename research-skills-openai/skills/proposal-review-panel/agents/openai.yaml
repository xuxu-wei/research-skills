interface:
  display_name: "Proposal Review Panel"
  short_description: "Run an isolated proposal review from one assigned role"
  default_prompt: "Use $proposal-review-panel for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
