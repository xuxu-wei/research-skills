interface:
  display_name: "Proposal Evaluator"
  short_description: "Independently evaluate a frozen proposal for significance, logic, evidence, methods..."
  default_prompt: "Use $proposal-evaluator for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
