interface:
  display_name: "Research Context Builder"
  short_description: "Normalize research inputs into an idea-generation brief"
  default_prompt: "Use $research-context-builder for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
