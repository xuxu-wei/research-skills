interface:
  display_name: "Sap Evaluator"
  short_description: "Independently evaluate a frozen Statistical Analysis Plan for endpoint alignment, data-method..."
  default_prompt: "Use $sap-evaluator for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
