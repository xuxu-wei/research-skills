interface:
  display_name: "Proposal Orchestrator"
  short_description: "Orchestrate research-proposal workflow from an existing idea or draft through context..."
  default_prompt: "Use $proposal-orchestrator for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: true
