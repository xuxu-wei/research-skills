interface:
  display_name: "Article Context Builder"
  short_description: "Normalize research inputs into an article context brief, classify study/article type, select..."
  default_prompt: "Use $article-context-builder for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
