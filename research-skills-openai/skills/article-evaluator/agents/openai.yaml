interface:
  display_name: "Article Evaluator"
  short_description: "Use when a frozen manuscript needs a holistic, non-compensatory independent evaluation across..."
  default_prompt: "Use $article-evaluator for this task and follow its workflow and output contract."
policy:
  allow_implicit_invocation: false
