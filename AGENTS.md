# AGENTS.md

## 维护范围

- 修改文件前读取距离目标文件最近的 AGENTS.md；子目录规则只在其范围内生效。
- 默认直接在 `main` 上工作，只保留 `main` 分支；只有用户明确要求时才创建其他分支。
- 仓库只维护 OpenAI 生态的 Skills 插件；唯一技能源为 research-skills-openai/skills/。保持现有插件名称和 Marketplace 身份。
- 新建或大幅改写 Skill 时使用 skill-creator；涉及插件结构或安装流程时使用 plugin-creator。
- 保留无关用户工作，不修改外部 Skill、个人安装配置或插件缓存。兼容导出和个人研究材料不纳入版本控制。

## Skill 编写

- 假设 agent 已有通用能力。Skill 聚焦研究流程、科学判断和必要的领域知识，不重复实现搜索、文件处理或平台调度。
- 主流程可整理输入、规划、撰写、修订和汇编。独立科学评审由未参与该版本撰写的新实例完成；评审者只读待评材料，不直接改写它。
- 科学内容发生实质变化后，保存新版本并重新独立评审。纯排版或措辞修改无需重启完整评审。无法独立评审时，仍可交付有用草稿，明确说明尚未独立评审。
- 保留关键来源、假设、未解决问题和有依据的异议。方向选择、资源投入和对外提交由用户决定。
- 核心流程放在 SKILL.md；领域细节放在有明确加载条件的 references/ 中。允许维护所有者指定的科研价值、科学依据、标书、SAP 和编辑决策领域标准；定义、计算和适用门槛只在标准资料中维护。分数必须联系证据与任务，不建立脱离证据的统一晋级规则，不重建工作流 Registry、状态协议或形式化报告模板。
- 面向研究者和用户的报告、表格、图注、摘要及附录使用自然语言和标准科学术语，不暴露内部字母缩写、方案代号、决策编号或门槛标签，也不采用代码加图例。解释具体项目、判断依据、评分含义与下一步；必要科学符号按科学含义保留并定义。

## 文档与验证

- README.md 是使用入口；根目录 ROADMAP.md 是唯一规划文档；开发与人工验证方法见 docs/development.md。
- Skill 的行为和科研质量由所有者人工测试。不维护自动 LLM 测试、历史工作流 Fixture，或断言措辞、标题、角色数量的测试。
- tests/scripts/ 只测试辅助自动化脚本，使用合成输入；运行 python -m unittest discover -s tests/scripts -v。
- 必要时使用官方 Skill/插件工具做一次格式检查；格式通过不能作为行为验证或人工验收的证明。
- 提交前检查变更范围、引用和版本说明，运行适用的脚本测试；报告未验证事项。个人研究输入和生成材料留在已忽略的 .local/ 下。
