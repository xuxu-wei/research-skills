Use the installed `research-skills-openai:research-opportunity-mapper` on the
attached frozen request `p8-l11.md`. First create a self-contained Deep Research
handoff and one opaque pending-edge ID, then pause. Do not perform, imitate, or
summarize the multi-stage research inline.

The user must explicitly start Deep Research. Continue only after the provider
run has completed and its real session/run identity, completion receipt, raw
output, citations, and timestamps are available. Return those artifacts through
the mapper contract, bind every material claim to identity-verified primary or
authoritative sources, and resume exactly the recorded edge once. Handoff,
user-start, provider-complete, mapper-return, and resume events must be strictly
ordered and use unique identities.

The current Preview capture bridge has no registered concrete parser for a
ChatGPT Deep Research event export. A downloaded Markdown, Word, or PDF report,
or a task-authored JSON carrying session/run labels, is content only and is not
provider-completion evidence. Unless a committed exact-schema adapter is
actually registered, preserve available content, write a self-contained
`continuation-package.yaml`, mark the completion projection pending, and stop;
do not create `provider-completed.json`, return the edge, or resume it.

Write UTF-8, machine-readable, `schema_version: 1` files under
`runs/p8-l11/capture/`: `task-export.json`, `mapper-handoff.yaml`,
`user-start-event.json`, `provider-completed.json`,
`raw-deep-research-output.json`, `citation-export.json`,
`mapper-return.yaml`, `resume-receipt.json`, `evidence-artifact-index.json`, and
`retrieval-receipt.json`. Bind plugin/source identity and every path, size, and
SHA-256 digest. Never invent a missing event or platform ID. Do not make an
external submission.
