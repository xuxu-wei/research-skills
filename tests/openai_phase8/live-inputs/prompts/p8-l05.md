Use the installed `research-skills-openai:perspective-evaluator` skill in
exactly one fresh delegated reviewer instance on the attached frozen source
`p8-l05.md`. The parent task is only a dispatcher and must not perform the
assessment inline.

Construct a source-only sanitized blind bundle. Give the delegated instance
only the frozen source plus the skill and its directly required resources. Do
not expose scheduler files, fixture collections, prior scores or decisions,
earlier reviewer output, other reviewer output, revision instructions, or any
scheduler-labelled finding. The source is read-only. The delegated instance may
write only `runtime-artifacts/reviews/p8-l05/report.yaml` and must report its
actual files read, decision, findings, unresolved issues, and dissent.

After the delegated instance returns, the dispatcher must preserve the raw
response and write UTF-8, machine-readable, `schema_version: 1` capture files:
`capture/task-export.json`, `capture/source-only-blind-bundle.yaml`,
`capture/dispatch-prompt.yaml`, `capture/raw-review-output.yaml`,
`capture/read-scope.json`, and `capture/reviewer-run.json`. Bind the real parent
task ID, delegated thread/run ID, reviewer instance ID and creation time,
platform receipt ID, plugin/source identity, exact prompt and source digests,
actual read/write sets, and identical source hashes before and after. Never
invent an unavailable platform identifier. Do not make an external submission.
