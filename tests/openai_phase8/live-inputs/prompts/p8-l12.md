Use the installed `research-skills-openai:research-opportunity-mapper` on the
attached frozen request `p8-l12.md`. Inspect the actual Deep Research capability
state before retrieval. This run is valid only on a surface where that
capability is inactive, unavailable, or cannot be started in the task.

When the capability is not active, emit a self-contained mapper handoff and
continuation package, record one opaque pending-edge ID, and pause. Do not
perform, imitate, or summarize Deep Research inline; do not create a downstream
evidence map or claim that research ran.

Write UTF-8, machine-readable, `schema_version: 1` files under
`runs/p8-l12/capture/`: `task-export.json`, `capability-state-export.json`,
`mapper-handoff.yaml`, `continuation-package.yaml`, and
`retrieval-receipt.json`. Bind the real task ID, plugin/source identity,
capability-state provenance, exact request digest, pause state, all paths,
sizes, and SHA-256 digests. Never invent a platform capability record or ID. Do
not make an external submission.
