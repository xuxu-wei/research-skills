Answer the attached frozen request `p8-l08.md` using ChatGPT/Codex built-in
Search. Do not substitute a local retrieval script, repository fixture, or an
uncited memory answer. Open at least two identity-verified primary or
authoritative sources, retain their canonical URLs or provider source IDs, and
map every material claim in the answer to one or more opened sources.

Preserve the actual tool trace and write UTF-8, machine-readable,
`schema_version: 1` capture files under `runs/p8-l08/capture/`:
`task-export.json`, `raw-search-output.json`, `citation-export.json`, and
`retrieval-receipt.json`. Bind the real task/tool/run IDs, plugin/source
identity, exact query and request digests, timestamps, opened-source identities,
claim-to-source mappings, output paths, sizes, and SHA-256 digests. Never invent
an unavailable platform identifier. Do not make an external submission.
