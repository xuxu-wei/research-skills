Use the installed `research-skills-openai:research-idea-orchestrator` public
entry in `standard` mode on the attached frozen file `p7-l02.md`.

Run the declared workflow using only the permitted scope in the source. Route
from source facts and independent findings; do not force a decision. Do not
inspect repository fixtures, scheduling files, receipt collections, or earlier
task outputs. Treat the attachment as read-only and record its artifact ID,
version, path, and SHA-256 before delegation.

Use a fresh delegated evaluator for the frozen candidate set and fresh
delegated adversarial roles only when their workflow gate is reached. Keep
reviews sealed, hide prior scores and suggested decisions, and allow reviewer
writes only to review-report paths. Do not evade the stated scope by inventing
a new endpoint, dataset, analysis, audience, or design. Apply the normal
revision and no-gain rules based on the independent report.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l02/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, visible findings and dissent, and all
file digests. Never invent a missing platform ID. These are capture-supporting
task artifacts, not verification receipts. Stop before
proposal drafting or external submission. Return only the observed workflow
state, artifact pointers, unresolved issues, and next human action.
