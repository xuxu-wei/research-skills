Explicitly invoke the installed
`research-skills-openai:research-polisher-orchestrator` public entry in
`standard` mode on the attached frozen file `p7-l09.md`.

Run the Research Polisher workflow from the completed study, current framing,
audience space, and bounded-work facts. Do not draft manuscript prose, execute
added work, or select a preferred strategy. Do not inspect repository fixtures,
scheduling files, receipt collections, or earlier task outputs. Record the
attachment's artifact ID, version, path, and SHA-256 and keep it read-only.

Dispatch the scientific-significance, practical-value, and
dissemination-editorial strategy roles to three mutually blind fresh instances.
Each must account for all three effort tiers. Assemble anonymously without
scoring or creating options, seal identity provenance, then use a different
fresh methodology/publishability reviewer that cannot read raw strategist
reports or identities. Any revised portfolio requires permitted fresh strategy
work, reassembly, and a fresh final review. Preserve conflicts and rejected
options.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l09/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, sealed provenance, findings, and all
file digests. Never invent a missing platform ID. These are capture-supporting
task artifacts, not verification receipts. Mark any
outlet-specific claim without verified requirements accordingly, stop before
executing added work or external submission, and return only the observed
workflow state, artifact pointers, unresolved issues, and next human action.
