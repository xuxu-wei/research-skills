Use the installed `research-skills-openai:perspective-orchestrator` public entry
in `full` mode on the attached frozen file `p7-l07.md`.

Run the full Perspective workflow from the thesis, bounded evidence packet,
counter-position, and delivery limits. Verify source identity before relying on
a material external claim, and weaken or remove unsupported generalizations.
Do not inspect repository fixtures, scheduling files, receipt collections, or
earlier task outputs. Record the attachment's artifact ID, version, path, and
SHA-256 and keep it read-only.

Delegate evaluation, each panel role, language assessment when applicable, and
final composition/verification to distinct fresh instances. Keep prior scores,
suggested decisions, and peer reports hidden. Reviewers may write only their
reports; source changes go to the owning drafter, create a new version and
delta, and require fresh evaluation. Preserve counterarguments, limitations,
and panel dissent through final assembly.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l07/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, dissent/findings, and all file
digests. Never invent a missing platform ID. These are capture-supporting task
artifacts, not verification receipts. Use only a generic outlet profile and
stop before external submission. Return the observed workflow state, artifact
pointers, unresolved issues, and next human action.
