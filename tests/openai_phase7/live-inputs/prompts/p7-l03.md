Use the installed `research-skills-openai:proposal-orchestrator` public entry in
`standard` mode on the attached frozen file `p7-l03.md`.

Run the proposal workflow end to end. Route solely from the supplied funding,
data, pilot, methods, and resource facts plus source-grounded external evidence
that is genuinely needed. Do not inspect repository fixtures, scheduling
files, receipt collections, or earlier task outputs. Freeze the attachment's
artifact ID, version, path, and SHA-256 and keep it read-only.

Dispatch readiness, proposal evaluation, each required panel role, and any
other reviewer role to distinct fresh delegated instances. Reviewers must not
see prior scores, expected decisions, peer reports, or outcome-bearing paths,
and may write only their reports. Route substantive fixes to the owning writer,
create a new proposal version and delta, then use a fresh evaluator. Preserve
all dissent and unresolved findings in the package.

The frozen call requires a statistical analysis plan. Dispatch `sap-writer`
on its registered conditional proposal edge as a `supporting_writer`, freeze
the generated SAP, and dispatch `sap-evaluator` as a distinct fresh delegated
`supporting_reviewer`. Package only the latest SAP version and the fresh SAP
evaluation whose lineage binds exactly that version. An external input cannot
stand in for either generated artifact or either actor.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l03/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, dissent/findings, and all file
digests. Never invent a missing platform ID. Honor every deliverable required by
the frozen call, including independently reviewed analytical attachments;
branches not required by the source or an independent gate remain optional.
These are capture-supporting task artifacts, not verification receipts. Do not
claim verified target rules and stop before external submission. Return the
observed workflow state, artifact pointers, unresolved issues, and next human
action.
