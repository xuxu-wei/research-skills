Use the installed `research-skills-openai:research-idea-orchestrator` public
entry in `standard` mode on the attached frozen file `p7-l01.md`.

Run the declared workflow end to end and route only from the source facts and
independent findings. Do not inspect repository fixtures, scheduling files,
receipt collections, or earlier task outputs. Treat the attachment as
read-only, record its artifact ID, version, path, and SHA-256 before delegation,
and do not invent unavailable data or evidence.

Use fresh delegated instances for every required evaluator and adversarial
role. Keep their reports mutually sealed as required, never expose prior scores
or suggested decisions, and allow reviewer writes only to review-report paths.
Any revised candidate set must receive a fresh evaluation. Preserve minority
views, fatal findings, and unresolved issues through assembly.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l01/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, artifact lineage, complete scoped
reads/writes, source before/after hashes, and file digests. Never invent a
missing platform ID. These are capture-supporting task artifacts, not
verification receipts.
Stop before proposal drafting or any external submission. Return only the
observed workflow state, artifact pointers, unresolved issues, and next human
action.
