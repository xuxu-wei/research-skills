Use the installed `research-skills-openai:proposal-orchestrator` public entry in
`existing_draft` mode on the attached frozen file `p7-l04.md`.

Treat the draft summary and dated custodian record as coequal frozen inputs.
Run the workflow through the applicable readiness and independent quality
gates, routing only from those facts. Do not inspect repository fixtures,
scheduling files, receipt collections, or earlier task outputs. Record the
attachment's artifact ID, version, path, and SHA-256 and keep it read-only.

Dispatch each quality judgment to a fresh delegated reviewer. Do not expose
prior scores, suggested decisions, peer reports, or scheduler information.
Reviewer writes are limited to review reports. Do not reinterpret the final
custodian notice as tentative, invent a substitute dataset, or silently change
the fixed project aim. Preserve every material finding and identify the
specific external fact needed for any later continuation.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l04/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, visible findings, continuation, and
all file digests. Never invent a missing platform ID. These are
capture-supporting task artifacts, not verification receipts. Stop
before external submission. Return only the observed workflow state, artifact
pointers, unresolved issues, and next human action.
