Use the installed `research-skills-openai:perspective-orchestrator` public entry
in `standard` mode on the attached frozen file `p7-l08.md`.

Run the workflow within the fixed thesis, evidence, length, and revision scope.
Route from source facts and independent findings; do not force a decision or
add material outside the authorized operations. Do not inspect repository
fixtures, scheduling files, receipt collections, or earlier task outputs.
Record the attachment's artifact ID, version, path, and SHA-256 and keep it
read-only.

Use a fresh delegated evaluator for the frozen draft and a different fresh
evaluator for any changed version. Hide prior scores, suggested decisions,
peer outputs, and scheduler information. Reviewers may write only review
reports. Route a revision only when it fits the permitted operations, record
its exact delta, and apply the normal no-gain rule from the independent
comparison. Do not introduce external evidence or an undeclared thesis.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l08/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, visible findings and dissent, and all
file digests. Never invent a missing platform ID. These are capture-supporting
task artifacts, not verification receipts. Stop before
external submission. Return only the observed workflow state, artifact
pointers, unresolved issues, and next human action.
