Explicitly invoke the installed
`research-skills-openai:research-polisher-orchestrator` public entry in
`standard` mode on the attached frozen file `p7-l10.md`.

Run the Research Polisher workflow using the published-work record, available
assets, and non-negotiable boundary exactly as supplied. Do not broaden the
requested genre, invent resources, propose unpermitted work, or hide the prior
publication. Do not inspect repository fixtures, scheduling files, receipt
collections, or earlier task outputs. Record the attachment's artifact ID,
version, path, and SHA-256 and keep it read-only.

Dispatch all three strategy perspectives to mutually blind fresh instances;
each must account honestly for every effort tier and may record the absence of
a defensible option rather than fabricate one. Assemble anonymously without
scoring, ranking, selecting, or creating hybrid plans. Use a different fresh
methodology/publishability reviewer that cannot see strategist identities or
raw reports. Preserve every disagreement, limitation, and rejected cell.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l10/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, sealed provenance, visible findings,
and all file digests. Never invent a missing platform ID. These are
capture-supporting task artifacts, not verification receipts. Stop before
added work or external submission. Return only the
observed workflow state, artifact pointers, unresolved issues, and next human
action.
