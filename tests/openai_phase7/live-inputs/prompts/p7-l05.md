Use the installed `research-skills-openai:article-orchestrator` public entry in
`standard` mode on the attached frozen file `p7-l05.md`.

Run the article workflow end to end from the completed computational benchmark.
Treat internal measurements and archive facts as investigator-supplied source
facts, preserve their claim boundary, and verify only external background
claims that materially need citations. Do not inspect repository fixtures,
scheduling files, receipt collections, or earlier task outputs. Record the
attachment's artifact ID, version, path, and SHA-256 and keep it read-only.

Delegate readiness, methods/statistics audit, claim audit, evaluation, each
panel role, and final package verification to distinct fresh instances. Keep
reviewer reports sealed as required; do not expose prior scores, peer reports,
or suggested decisions. Reviewers may write only their reports. Route any
source-text change through the owning writer, version it, record a delta, and
obtain a fresh evaluation before packaging. Preserve dissent and limitations.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l05/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, dissent/findings, and all file
digests. Never invent a missing platform ID. These are capture-supporting task
artifacts, not verification receipts. Use a generic outlet profile, do not
invent measurements or verified journal requirements, and stop before external
submission. Return the observed workflow state, artifact pointers, unresolved
issues, and next human action.
