Use the installed `research-skills-openai:article-orchestrator` public entry in
`fast_track_draft` mode on the attached frozen file `p7-l06.md`.

Treat the original analysis summary, reproducibility audit, and recoverability
record as one frozen source. Run the applicable readiness, methods, claims, and
evaluation gates and route only from those facts. Do not inspect repository
fixtures, scheduling files, receipt collections, or earlier task outputs.
Record the attachment's artifact ID, version, path, and SHA-256 and keep it
read-only.

Use distinct fresh delegated instances for every quality judgment. Reviewers
must not see prior scores, suggested decisions, peer reports, or scheduler
information and may write only their reports. Do not conceal the full-data
feature-selection procedure, treat contaminated rows as untouched, invent a
replacement cohort, or convert the fixed article question into another genre.
Keep all material findings visible and bind any continuation to the exact
missing source asset.

The task workspace has been staged outside the source repository. Store
versioned outputs under `runs/p7-l06/`. Before returning, write real UTF-8 JSON
files `capture/task-export.json`, `capture/actor-manifest.json`,
`capture/artifact-index.json`, and `capture/file-access.json`, each with
`schema_version: 1`. Bind the actual parent task/thread ID, every observable
child task/thread/run or reviewer instance ID, plugin/registry/commit identity,
observed workflow classification and state, complete lineage and scoped
reads/writes, source before/after hashes, visible findings, continuation, and
all file digests. Never invent a missing platform ID. These are
capture-supporting task artifacts, not verification receipts. Stop before
external submission and
return only the observed workflow state, artifact pointers, unresolved issues,
and next human action.
