name: OpenAI Plugin Preview

on:
  push:
    branches: [main, 'codex/**']
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    # Preserve the existing required-check name; test auxiliary scripts only.
    name: OpenAI Plugin Preview / validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'
      - name: Test auxiliary scripts
        env:
          PYTHONUTF8: '1'
        run: python -m unittest discover -s tests/scripts -v
